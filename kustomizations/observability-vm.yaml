apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: observability-vm
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./infrastructure/observability/victoriametrics
  prune: true
  wait: true
  dependsOn:
    - name: observability-vm-operator
    - name: observability-exporters
    - name: external-secrets
