apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: apps-directus
  namespace: flux-system
spec:
  interval: 10m
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./apps/directus
  prune: true
  wait: true
  dependsOn:
    - name: external-secrets
    - name: apps-postgres
    - name: apps-minio
